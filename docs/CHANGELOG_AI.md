# Changelog (AI-assisted changes)

## [Unreleased]

### Added

- **Registry Editor — Past Editions carousel file pickers**
  - **Goal:** Replace URL text inputs for edition images with per-slide file pickers, preview, add/remove slides, and dev-time writes to `public/past-editions/`; keep `edition.image` as a string URL (path or data URL).
  - **Changes:** (1) `src/lib/past-edition-image-upload.ts` — validation (image/*, max 5 MB), `resolvePastEditionImageUrl` (dev POST to `__registry-upload-past-edition`, else data URL). (2) `vite.config.ts` — dev-only middleware writes base64 payload to `public/past-editions/`. (3) `src/pages/RegistryEditor.tsx` — `renderPastEditionsCarouselContent`, filter generic content rows under `pastEditions.editions.*`. (4) `src/config/style-registry.ts` — `pastEditions.cards` array aligned with default edition count. (5) `public/past-editions/.gitkeep`. (6) Tests: `past-edition-image-upload.test.ts`, `RegistryEditor.test.tsx` assertions.
  - **Verification:** `npx vitest run src/lib/past-edition-image-upload.test.ts src/pages/RegistryEditor.test.tsx src/components/PastEditionsSection.test.tsx` passes.

- **Admin-only Export Users CSV**
  - **Goal:** One-button export of all customer data (profiles, subscription, orders, notes) into a single CSV for admin operations; backend enforcement via RLS only.
  - **Changes:** (1) `docs/supabase_admin_export_customers_view.sql` — view `admin_export_customers` (one row per user). (2) `src/lib/admin-export-csv.ts` — chunked fetch, CSV build with human-readable headers and `users_export_YYYY-MM-DD_HH-mm.csv` filename. (3) `src/pages/admin/AdminCustomers.tsx` — "Export Customer Data" button, loading/success/error UX; "Export CSV (page)" kept. (4) `src/lib/admin-export-csv.test.ts` — 5 tests for fetch, chunking, CSV build, download flow, and error. (5) `docs/PLAN_EXPORT_USERS_CSV.md` — plan and field mapping.
  - **Verification:** `npm run test -- --run src/lib/admin-export-csv.test.ts` passes. Run the new SQL in Supabase after Phase 2; as admin, use Export Customer Data and open CSV in Excel/Sheets.

- **Local dev server port hardening for intermittent localhost 404s**
  - **Goal:** Stop intermittent `npm run dev` 404s caused by local port collisions by moving this project off the conflicted `8080` port and making startup fail loudly when the chosen port is unavailable.
  - **Changes:** (1) `vite.config.ts`: changed the dev server host to `127.0.0.1`, moved the default port to `5180`, and enabled `strictPort: true` so this project binds to a single predictable local endpoint instead of competing with unrelated local servers. (2) `src/dev-server-config.test.ts`: added a regression test that asserts the checked-in Vite config keeps the dedicated port and strict binding in place. (3) `docs/FAILURE_LOG.md`: documented the red-phase test discovery/import failures encountered while locking down the config test.
  - **Verification:** `npm run test -- src/dev-server-config.test.ts` passes. The active Vite dev server returns `200` for `http://127.0.0.1:5180/` and `http://127.0.0.1:5180/login`.

- **Auth warning cleanup and final customer-facing Spanish sweep**
  - **Goal:** Remove auth-related test warning noise at the root and finish the last visible English strings on customer-facing auth/loading states without touching admin pages.
  - **Changes:** (1) `src/test/render-with-auth.tsx`: added a shared auth test helper that provides a fixed `AuthContext` value and `MemoryRouter` future flags so page tests no longer mount the live `AuthProvider`. (2) `src/pages/Login.test.tsx`, `src/pages/Dashboard.test.tsx`, `src/pages/Account.test.tsx`, `src/pages/OrderHistory.test.tsx`, `src/pages/SubscriptionManagement.test.tsx`, `src/pages/SettingsPage.test.tsx`, and `src/pages/NotificationsPage.test.tsx`: migrated auth-aware page tests to the helper to eliminate auth setup warnings while preserving assertions. (3) `src/config/app-registry.ts`: added `status.loading`, `login.submitLoading`, translated auth email placeholders to `tu@correo.com`, and localized remaining customer-facing checkout `Email`/`email` copy. (4) `src/pages/Login.tsx`, `src/pages/ResetPassword.tsx`, and `src/components/ProtectedRoute.tsx`: now render registry-driven Spanish loading text (`Iniciando sesion...`, `Cargando...`) instead of hardcoded English strings. (5) Added focused coverage in `src/pages/ResetPassword.test.tsx` and `src/components/ProtectedRoute.test.tsx`, and expanded `src/config/app-registry.localization.test.ts` plus `src/pages/Login.test.tsx` to lock the new Spanish auth copy.
  - **Verification:** `npm run test -- src/config/app-registry.localization.test.ts src/pages/Login.test.tsx src/pages/ResetPassword.test.tsx src/components/ProtectedRoute.test.tsx src/pages/Dashboard.test.tsx src/pages/Account.test.tsx src/pages/OrderHistory.test.tsx src/pages/SettingsPage.test.tsx src/pages/NotificationsPage.test.tsx src/pages/SubscriptionManagement.test.tsx` passes (`20` tests).

- **Spanish localization sweep for auth, dashboard, delivery windows, and visible UI copy**
  - **Goal:** Translate the remaining visible English copy across the customer-facing site into Spanish, including auth flows, dashboard quick actions, delivery-window labels/month formatting, navigation fallbacks, and other UI strings, while preserving niche product words like `skincare`.
  - **Changes:** (1) `src/config/app-registry.ts`: translated remaining English content in `pastEditions`, `experience.subtitle`, `login`, `register`, `forgotPassword`, `resetPassword`, `dashboard`, `orderHistory`, `subscriptionManagement`, `settings`, `notifications`, and `account`. (2) `src/lib/delivery-windows.ts`: localized the half-window domain values to `Primera quincena` / `Segunda quincena`. (3) `src/components/DeliveryWindowsSection.tsx`: switched month formatting to `es-MX` and localized rendered delivery labels to `de <mes> <anio>`. (4) `src/components/Navbar.tsx`: localized remaining hardcoded fallbacks and the mobile menu aria-label. (5) `src/pages/Dashboard.tsx`, `src/pages/OrderHistory.tsx`, `src/pages/SubscriptionManagement.tsx`, `src/pages/SettingsPage.tsx`, `src/pages/NotificationsPage.tsx`, and `src/pages/Account.tsx`: localized back-link aria-labels and switched visible date formatting to `es-MX` where applicable. (6) `src/components/TestimonialsSection.tsx`: localized testimonial navigation aria-labels. (7) `src/lib/test-user-data.ts`: translated the default subscription plan label to `KumiBox mensual`. (8) `src/pages/RegistryEditor.tsx`: localized section labels for `pastEditions`, `resetPassword`, and `account`. (9) Added regression coverage in `src/config/app-registry.localization.test.ts` and expanded existing dashboard/delivery/page tests to lock the Spanish behavior.
  - **Verification:** `npm run test -- src/config/app-registry.localization.test.ts src/lib/delivery-windows.test.ts src/components/DeliveryWindowsSection.test.tsx src/pages/Dashboard.test.tsx src/pages/OrderHistory.test.tsx src/pages/SubscriptionManagement.test.tsx src/pages/SettingsPage.test.tsx src/pages/NotificationsPage.test.tsx` passes (`22` tests). `npm run build` passes.

- **Hostinger deployment package and upload checklist**
  - **Goal:** Prepare a ready-to-upload static deployment package for Hostinger so the Vite site can be published without accidentally uploading the wrong folder structure or breaking SPA routes.
  - **Changes:** (1) Refreshed `Hostinger/` from the latest production `dist` build so it contains the exact files that should be uploaded to `public_html`. (2) Added `Hostinger/.htaccess` to provide Apache SPA fallback for React Router routes like `/login` and `/reset-password`. (3) Added `Hostinger/UPLOAD_CHECKLIST.md` with step-by-step Hostinger upload instructions, route verification steps, and Supabase production URL reminders.
  - **Verification:** `npm run build` passes. `Hostinger/index.html` points at the latest hashed bundle files and the package now includes `.htaccess` plus the upload checklist.

- **Delivery windows preview section ("When will I receive my Skincare?")**
  - **Goal:** Add a reusable, interactive landing-page section that lets customers choose a subscription date and preview the next 6 bi-monthly delivery windows by half-month period, without exposing exact delivery dates.
  - **Changes:** (1) `src/config/app-registry.ts`: added `deliveryWindows` registry content for title, Spanish description, date-picker label, subscription group label, half labels, and delivery list heading. (2) `src/config/style-registry.ts`: added `deliveryWindows` section styles and divider config, plus `DeliveryWindowsStyles` type. (3) `src/lib/delivery-windows.ts`: added deterministic delivery-window calculation helpers (`getHalfFromDay`, `getDeliveryWindows`) with month rollover handling. (4) `src/components/DeliveryWindowsSection.tsx`: new premium-styled interactive section with date picker, instant updates, subscription-group display, and six formatted windows. (5) `src/pages/Index.tsx`: inserted the new section between `ExperienceSection` and `TestimonialsSection` in page order, with its own divider. (6) `src/lib/apply-style-registry.ts`: added `--deliveryWindows-section-bg` runtime style bridge. (7) `src/pages/RegistryEditor.tsx`: added `deliveryWindows` to the editable section list and section-style controls. (8) `src/components/DeliveryWindowsSection.test.tsx` and `src/lib/delivery-windows.test.ts`: added component and logic coverage. (9) `src/components/TestimonialsSection.test.tsx`: aligned the test with the intentionally hidden Testimonials final state so suite expectations match product behavior.
  - **Verification:** `npm run test -- --run` passes (36 tests). `npm run build` passes. Live browser verification confirmed the delivery section logic for First Half and Second Half examples and confirmed the mobile sticky-header overlap fix. Per request, Testimonials was temporarily enabled for live verification and then restored to hidden (`SHOW_TESTIMONIALS = false`) in the final state.

- **Mobile-only Past Editions + Experience optimizations**
  - **Goal:** Past Editions selected card expands vertically on mobile; Experience cards at least 1/3 viewport width on mobile with tighter spacing; all changes mobile-only (desktop unchanged).
  - **Changes:** (1) **PastEditionsSection:** Active card on mobile uses `max-md:min-h-[40vh] max-md:max-h-[55vh]`; inactive stay `h-40`. (2) **ExperienceSection:** Mobile-only: section `max-md:px-4`; timeline and dot `left-4`; step row `max-md:gap-4 max-md:mb-4`; card wrapper `max-md:min-w-[33.33vw]` and `max-md:pl-4`; inner card `max-md:p-4`. Desktop layout and spacing unchanged.
  - **Files touched:** `src/components/PastEditionsSection.tsx`, `src/components/ExperienceSection.tsx`, `workflow/ChangeLog.md`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm run build` succeeds.

- **Full mobile responsiveness audit and implementation**
  - **Goal:** Make the site adapt correctly on 320px–414px and tablet without breaking desktop; no horizontal scroll, readable text, tappable buttons.
  - **Audit:** `docs/MOBILE_RESPONSIVENESS_AUDIT.md` — viewport OK; critical: Navbar (no mobile menu), AdminCustomers table overflow, AdminLayout nav, TestimonialsSection orbit container, ExperienceSection step width; moderate: Hero/Problem/HowItWorks typography, CompatibilityTest/FinalCTA/Pricing spacing.
  - **Changes:** (1) **Navbar:** Mobile menu via Sheet + hamburger (Menu icon) below `md`; nav links and auth (Log in / Get started / Account / Log out) in sheet; desktop layout unchanged. (2) **AdminCustomers:** Table wrapped in `overflow-x-auto`, table `min-w-[800px]` for horizontal scroll on small screens. (3) **AdminLayout:** Header flex-wrap and stacked on small screens. (4) **TestimonialsSection:** Orbit container size capped to `min(baseSize, window.innerWidth - 32)` with resize listener. (5) **ExperienceSection:** Step content `max-w-full md:max-w-[calc(50%-2rem)]` (removed fixed 50% on mobile). (6) **HeroSection:** Title `text-4xl sm:text-5xl md:text-7xl`. (7) **ProblemSection:** Title `text-3xl sm:text-4xl md:text-6xl`. (8) **HowItWorksSection:** Step cards `w-48 h-48 sm:w-64 sm:h-64 md:w-72 md:h-72 max-w-full`. (9) **CompatibilityTestSection:** Yes/No buttons `flex-col sm:flex-row`, `min-h-[44px]`. (10) **FinalCTASection:** `py-16 md:py-32`, `px-4 sm:px-6`. (11) **PricingSection:** Cards `p-6 sm:p-8`. (12) **Navbar container:** `px-4 sm:px-6`.
  - **Files touched:** `src/components/Navbar.tsx`, `src/pages/admin/AdminCustomers.tsx`, `src/components/AdminLayout.tsx`, `src/components/TestimonialsSection.tsx`, `src/components/ExperienceSection.tsx`, `src/components/HeroSection.tsx`, `src/components/ProblemSection.tsx`, `src/components/HowItWorksSection.tsx`, `src/components/CompatibilityTestSection.tsx`, `src/components/FinalCTASection.tsx`, `src/components/PricingSection.tsx`, `docs/MOBILE_RESPONSIVENESS_AUDIT.md`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm run build` succeeds. Manual responsive testing recommended at 320×568, 375×667, 390×844, 414×896, and tablet. No FAILURE_LOG entry.

- **Account page: registry-driven profile, security, and preferences**
  - **Goal:** Provide a dedicated Account page using registry-driven text that shows profile details, security actions, and a preferences placeholder while matching the dashboard's protected layout.
  - **Changes:** (1) `src/config/app-registry.ts`: Expanded `account` section with subtitle, profile/security/preference labels, and link targets (`/dashboard`, `/forgot-password`). (2) `src/pages/Account.tsx`: Implemented Account page shell (Navbar, padded main content, max-width column) with read-only profile summary, Security card (Change password), Preferences card (coming soon), and Back to Dashboard action; all copy pulled from `getSectionContent("account")` and user data from `useAuth()`. (3) `src/pages/Account.test.tsx`: New tests asserting headings and that Edit profile, Change password, and Back to Dashboard links resolve to the URLs defined in `APP_REGISTRY.account`.
  - **Files touched:** `src/config/app-registry.ts`, `src/pages/Account.tsx`, `src/pages/Account.test.tsx`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `src/pages/Account.test.tsx` added (run via your Vitest/Vitest command). No new FAILURE_LOG entry.

- **Past Editions carousel: placeholder images and registry-driven image URLs**
  - **Goal:** Show pictures on each Past Editions carousel card and make all image URLs editable via the registry and RegistryEditor.
  - **Changes:** (1) **app-registry.ts:** Added `pastEditions.fallbackImage` (URL used when an edition has no image or load fails). Added `image` to each entry in `pastEditions.editions` with placeholder URLs (picsum.photos seeds per edition). (2) **PastEditionsSection.tsx:** Each card now renders an `<img>` with `src` from `edition.image` or `data.fallbackImage`; `onError` sets `src` to `fallbackImage`. Image is absolutely positioned to fill the card with object-cover; active card uses full color and slight scale, inactive cards use grayscale. (3) **PastEditionsSection.test.tsx:** New test "renders one image per edition card" to guard against regressions. (4) **RegistryEditor:** No code change; `getContentEntries` already walks section content, so `pastEditions.fallbackImage` and `pastEditions.editions[i].image` appear as editable Content rows for Past Editions.
  - **Files touched:** `src/config/app-registry.ts`, `src/components/PastEditionsSection.tsx`, `src/components/PastEditionsSection.test.tsx`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm test -- PastEditionsSection.test.tsx` passes (3 tests). Manual: open Registry Editor → Past Editions → Content; edit `fallbackImage` or any `editions[i].image` → Save; main page carousel shows updated images.

- **RegistryEditor: Preview button, Save to source files (app-registry.ts + style-registry.ts), date-stamped backups**
  - **Goal:** Preview applies changes in browser only (localStorage + reload). Save writes content and style to `src/config/app-registry.ts` and `src/config/style-registry.ts` with a safety backup before overwriting.
  - **Changes:** (1) **Preview button:** New "Preview" button in RegistryEditor applies current content, style, and contentModifiers to localStorage and reloads (same as previous Save behavior). (2) **Save button:** POSTs `{ content, style }` to dev-only `POST /__registry-save-source`. Vite plugin (dev only) creates `src/config/backups/` if missing; if no backup file with today’s date exists, copies current `app-registry.ts` and `style-registry.ts` to `backups/app-registry.YYYY-MM-DD.ts` and `backups/style-registry.YYYY-MM-DD.ts`, then overwrites the two source files with serialized content and style. (3) **Backups folder:** `src/config/backups/` added with `.gitkeep`; date-stamped backups are created once per day before the first Save of that day. (4) **RegistryEditor:** Save on success also writes to localStorage and reloads; on failure shows toast and does not reload.
  - **Files touched:** `src/pages/RegistryEditor.tsx`, `vite.config.ts`, `src/config/backups/.gitkeep`, `docs/CHANGELOG_AI.md`, `docs/ADMIN_REGISTRY_EDITOR.md`.
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG.

- **Main page not reflecting Registry Editor saves (registry.json) — fix**
  - **Issue:** After editing in Registry Editor and Save, the main page did not show the new values; it still showed defaults or old content/style.
  - **Root cause:** When registry is loaded from `registry.json` (file), `Index` on mount was calling `refresh()`, `refreshStyleRegistry()`, and `applyStyleRegistry(getMergedStyleRegistry())`. Those overwrote file-sourced state and DOM with **localStorage** (and defaults), so the file’s content/style were replaced before render.
  - **Changes:** (1) **RegistryContentContext:** When `initialContent` is provided (from file), `refresh()` is a no-op so content/contentModifiers are not overwritten by localStorage. Context now exposes `fromFile` so consumers know the source. (2) **StyleRegistryContext:** When `initialStyle` is provided (from file), `refreshStyleRegistry()` is a no-op so registry state is not overwritten by `getMergedStyleRegistry()`. (3) **Index:** When `fromFile` is true, the mount effect skips `applyStyleRegistry(getMergedStyleRegistry())`, `refresh()`, and `refreshStyleRegistry()`, so the main page keeps the registry.json values applied at bootstrap.
  - **Files touched:** `src/contexts/RegistryContentContext.tsx`, `src/contexts/StyleRegistryContext.tsx`, `src/pages/Index.tsx`, `docs/CHANGELOG_AI.md`.
  - **Verification:** Build succeeds. No FAILURE_LOG. Manual: edit in Registry Editor → Save → reload → main page shows saved content/style.

- **Registry Editor: file-based persistence (registry.json)**
  - **Goal:** Registry and app use a single source of truth; Save writes to a file so the next load (any browser) uses the latest values instead of depending on browser memory/localStorage.
  - **Changes:** (1) **Bootstrap:** `main.tsx` fetches `/registry.json`; on success uses it as the registry, else `getDefaultRegistry()` from `src/config/get-default-registry.ts` (content + style + contentModifiers from app-registry and style-registry). App receives `initialRegistry` and passes `initialContent`/`initialContentModifiers` to `RegistryContentProvider` and `initialStyle` to `StyleRegistryProvider`. (2) **Contexts:** `RegistryContentContext` accepts optional `initialContent` and `initialContentModifiers`; when provided uses them as full source (no localStorage merge). Exposes `fullContentForEditor` for the editor. `StyleRegistryContext` accepts optional `initialStyle`. (3) **Vite (dev only):** Plugin handles `POST /__registry-save` and writes request body to `public/registry.json`. (4) **Registry Editor:** Initializes state from context (one-time sync). Save POSTs `{ content, style, contentModifiers }` to `/__registry-save` then `window.location.reload()`. Reset restores defaults in editor only; user clicks Save to persist. (5) **Docs:** `docs/ADMIN_REGISTRY_EDITOR.md` updated (where registry loads from, Save/Reset behavior, dev-only save endpoint).
  - **Files touched:** `src/config/get-default-registry.ts` (new), `src/main.tsx`, `src/App.tsx`, `src/contexts/RegistryContentContext.tsx`, `src/contexts/StyleRegistryContext.tsx`, `src/pages/RegistryEditor.tsx`, `vite.config.ts`, `docs/ADMIN_REGISTRY_EDITOR.md`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG. Optional: run `npm run dev`, open Registry Editor, edit, Save → page reloads and `public/registry.json` is created/updated; reopen in another tab and confirm same content.

- **Registry variables: nav/hero/whatYouReceive emojis and badges; What You Receive card fill from palette**
  - **Plan:** docs/PLAN_REGISTRY_EDITOR.md (registry variables alignment); workflow Prompt.md.
  - **Changes:** (1) **app-registry.ts:** nav `logoEmoji`, `announcementBarEmoji`; hero `badgeText`, `imageBadgeLeft`, `imageBadgeRight`; whatYouReceive `subtitleBadgeEmoji`, products[].`emoji`. (2) **style-registry.ts:** WhatYouReceiveStyles `cards: Array<{ backgroundIndex }>` (5 entries, default indices 8,10,11,12,16). (3) **apply-style-registry.ts:** sets `--whatYouReceive-card-0-bg` … `--whatYouReceive-card-4-bg` from palette; uses registry.whatYouReceive.cards ?? STYLE_REGISTRY defaults. (4) **Components:** Navbar uses announcementBarEmoji, logoEmoji; HeroSection uses badgeText, imageBadgeLeft, imageBadgeRight; WhatYouReceiveSection uses subtitleBadgeEmoji, products[].emoji, card backgrounds from CSS vars. (5) **RegistryEditor:** What You Receive → Style: “Product card fill colors” with five palette selects (Card 1–5); content for new keys picked up by existing getContentEntries walker.
  - **Files touched:** `src/config/app-registry.ts`, `src/config/style-registry.ts`, `src/lib/apply-style-registry.ts`, `src/components/Navbar.tsx`, `src/components/HeroSection.tsx`, `src/components/WhatYouReceiveSection.tsx`, `src/pages/RegistryEditor.tsx`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG (no failure). Optional: visual check—edit nav/hero/whatYouReceive in editor, Save, reload home to confirm persistence.

- **RegistryEditor: apply all tab elements on main page (style, content, images)**
  - **Issue:** Changes saved in RegistryEditor were not applied on the main page after Save (same-tab); text came from APP_REGISTRY only; hero/why/howItWorks images were missing from the editor and not driven by registry.
  - **Changes:** (1) **Same-tab re-apply:** `Index` on mount calls `applyStyleRegistry(getMergedStyleRegistry())`, `refresh()` (content), and `refreshStyleRegistry()` so edits in the editor apply when navigating to `/`. (2) **StyleRegistryContext:** New `src/contexts/StyleRegistryContext.tsx` provides merged style registry in React (for dividers and image paths); `Index` uses `useStyleRegistry().registry` for divider config. (3) **Merged content:** `RegistryContentContext` exposes `getSectionContent(sectionKey)` returning saved overrides merged with APP_REGISTRY; all section components and pages (Navbar, Hero, Problem, HowItWorks, CompatibilityTest, WhatYouReceive, PastEditions, Experience, Testimonials, Pricing, FAQ, FinalCTA, Footer; Login, Register, ForgotPassword, ResetPassword, Dashboard, Checkout, CheckoutSuccess, CheckoutCancel, Account) now use `getSectionContent()` instead of `APP_REGISTRY` for text. (4) **Images:** HeroSection uses `useStyleRegistry().registry.hero?.image?.path` for `img` src (fallback to imported asset); ProblemSection and HowItWorksSection use `registry.why.images[i].path` and `registry.howItWorks.images[i].path` with fallback to imported assets. (5) **RegistryEditor image controls:** Hero Style tab: Hero Image card (path, height, border radius, border color, border width). Why Style tab: Why/Problem images card (path per image). How it works Style tab: Step images card (path per step). (6) **Content modifiers:** HeroSection, PricingSection, ProblemSection (why), HowItWorksSection use `getStyleForPath()` for applicable content; other sections use merged content and can add getStyleForPath per path as needed.
  - **Files touched:** `src/contexts/StyleRegistryContext.tsx` (new), `src/contexts/RegistryContentContext.tsx`, `src/App.tsx`, `src/pages/Index.tsx`, `src/components/HeroSection.tsx`, `src/components/Navbar.tsx`, `src/components/ProblemSection.tsx`, `src/components/HowItWorksSection.tsx`, `src/components/CompatibilityTestSection.tsx`, `src/components/WhatYouReceiveSection.tsx`, `src/components/PastEditionsSection.tsx`, `src/components/ExperienceSection.tsx`, `src/components/TestimonialsSection.tsx`, `src/components/PricingSection.tsx`, `src/components/FAQSection.tsx`, `src/components/FinalCTASection.tsx`, `src/components/FooterSection.tsx`, `src/pages/Checkout.tsx`, `src/pages/CheckoutSuccess.tsx`, `src/pages/CheckoutCancel.tsx`, `src/pages/Login.tsx`, `src/pages/Register.tsx`, `src/pages/ForgotPassword.tsx`, `src/pages/ResetPassword.tsx`, `src/pages/Dashboard.tsx`, `src/pages/Account.tsx`, `src/pages/RegistryEditor.tsx`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG (no failure).

- **RegistryEditor Phase 5 – Testing & docs**
  - **Plan:** docs/PLAN_REGISTRY_EDITOR.md Phase 5 (workflow Prompt.md execution).
  - **5.1–5.3 Testing doc:** Added `docs/REGISTRY_EDITOR_TESTING.md` with key breakpoints (375, 768, 1280), visual/functional parity steps for main site (Hero, Pricing, one other section) and Registry Editor, responsive checklist for editor and previews, and critical path: edit (color, font, button/section, divider, content modifier) → Save → reload main site → verify. Optional E2E/smoke test noted as follow-up.
  - **5.4 Logs:** No failure during Phase 5; no FAILURE_LOG update. CHANGELOG_AI.md updated (this entry).
  - **5.5 Failsafe:** Added `docs/REGISTRY_EDITOR_FAILSAFE.md` (registry-first rule, convention for new/removed work, optional audit script as follow-up). Linked from `docs/ADMIN_REGISTRY_EDITOR.md` new § "Failsafe / registry-first rule."
  - **Files touched:** `docs/REGISTRY_EDITOR_TESTING.md` (new), `docs/REGISTRY_EDITOR_FAILSAFE.md` (new), `docs/ADMIN_REGISTRY_EDITOR.md` (failsafe section), `docs/PLAN_REGISTRY_EDITOR.md` (checkboxes 5.1–5.5), `docs/CHANGELOG_AI.md`.
  - **Verification:** Manual run of testing doc steps recommended; no automated E2E added in this phase.

- **Pricing section: apply content modifiers so RegistryEditor text style (color/font/size) shows on main page**
  - **Issue:** Changing text style for the pricing section in RegistryEditor and saving did not apply on the main page after refresh; only Hero was wired to content modifiers.
  - **Root cause:** PricingSection (and other sections) used only `APP_REGISTRY` and never `useRegistryContent()` or modifiers.
  - **Changes:** (1) `src/contexts/RegistryContentContext.tsx`: Exported `styleFromModifier()`; added `getStyleForPath(pathKey, defaultColorVar?)` to context state and `useRegistryContent()` return so any section can apply modifier-based styles. (2) `src/components/PricingSection.tsx`: Uses `useRegistryContent()` and `getStyleForPath()` for all pricing text: `pricing.subtitle`, `pricing.title`, `pricing.plans.{i}.name`, `description`, `priceAmount`, `pricePeriod`, `features.{i}`, `ctaButton`, `badge`, and `pricing.shippingLabel`, `shippingValue`, `commitmentLabel`, `commitmentValue`. Path keys match RegistryEditor `getContentEntries` (dot-joined paths).
  - **Verification:** No linter errors. Build succeeds. No FAILURE_LOG (no failure).

- **RegistryEditor plan 3.4, 3.5, 4.2: Two-way sync, Save/Reset confirmed, bootstrap merged style**
  - **Plan:** docs/PLAN_REGISTRY_EDITOR.md Phase 3.4, 3.5, 4.2 (workflow Prompt.md execution).
  - **3.4 Two-way sync:** RegistryEditor now applies the current style registry to the DOM whenever `registry` state changes. `useEffect` in RegistryEditor calls `applyStyleRegistry(registry)` so the rest of the app (and editor preview) sees style updates immediately without requiring Save. Persist to localStorage remains on Save.
  - **3.5 Save/Reset:** Confirmed already implemented; Save persists style, content, and content modifiers; Reset restores defaults and clears localStorage. Export/Import JSON left as optional/future.
  - **4.2 Bootstrap:** App bootstrap now loads saved style overrides from localStorage and applies merged registry. `src/lib/apply-style-registry.ts`: added `STYLE_STORAGE_KEY` export and `getMergedStyleRegistry()` (reads `app_registry_style_overrides`, merges with `STYLE_REGISTRY`, returns merged). Removed obsolete `registry.general.fonts.importUrl` block (fonts are now an array). `src/main.tsx`: calls `applyStyleRegistry(getMergedStyleRegistry())` instead of `applyStyleRegistry(STYLE_REGISTRY)` so the main site reflects saved theme on load. RegistryEditor imports `STYLE_STORAGE_KEY` from apply-style-registry to avoid duplication.
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG entry (no failure). Content context for merged text (main app reading saved content) remains pending per plan note.

- **RegistryEditor: Content modifiers (font, size, color) per text row + documentation**
  - **User request:** (1) Document all changes. (2) In the same row as each text input, add dropdowns for Font family, size, and color.
  - **Changes:** (1) `src/pages/RegistryEditor.tsx`: Added **content modifiers** state (`contentModifiers: Record<string, { fontFamily?, fontSize?, colorIndex? }>`) keyed by content path (e.g. `hero.heading`, `hero.rotatingQuotes.0`). Each Content row now shows one row: **text input** (flex-1) + **Font family** Select (from registry fonts + scanned fonts) + **Font size** Select (preset rem: 0.75rem–3rem) + **Color** Select (palette index with swatch). Persisted in `localStorage` under `app_registry_content_modifiers`; **Save** writes style, content, and content modifiers; **Reset** clears all three. Constants: `CONTENT_MODIFIERS_STORAGE_KEY`, `FONT_SIZE_PRESETS`, type `ContentModifiers`. (2) `docs/ADMIN_REGISTRY_EDITOR.md`: New § "Editor features" describing General tab, section tabs (Style + Content with per-row text and font/size/color modifiers), and Save/Reset keys. (3) `docs/CHANGELOG_AI.md`: This entry.
  - **Verification:** No linter errors; build succeeds.

- **RegistryEditor Phase 3.2 adjustments: Color palette always-visible text, Font list system, Layout refinements**
  - **User feedback:** (1) Color palette: Make text always visible (not just on hover) with Hex-Name-Comment on left, ID on top-right, using contrasting color. (2) Fonts: Change from single importUrl to a list system where first font = default, add new fonts via dropdown populated from `public/fonts/` folder, with refresh button to scan for new fonts. (3) Color palette layout: Text on bottom-left, each value on its own row (Hex value, Name, Comments), padding so first row starts at bottom third of box. (4) Font preview: Add phrase "Kumi box shows the font grady" to the right of each font row to showcase selected font.
  - **Changes:** (1) `src/pages/RegistryEditor.tsx`: Updated color palette cells to always show text with `getContrastColor()` function calculating luminance to determine white/black text. Display format: bottom-left shows three rows (Hex value, Name, Comments) with `pb-3` padding and `space-y-0.5` spacing, positioned absolutely at bottom; top-right shows index ID. (2) Font system: Changed `registry.general.fonts` from `{display, body, importUrl}` to array `Array<{name: string, isDefault?: boolean}>`. Added `scanAvailableFonts()` function that fetches `/fonts/custom-fonts.css` and parses `@font-face` declarations to extract font-family names. UI shows list of fonts with Select dropdowns, "Default" badge on first font, "Add Font" button, "Refresh Fonts" button (with loading spinner), delete (X) button per font (disabled if only one), and preview phrase "Kumi box shows the font grady" styled with `fontFamily: font.name` to showcase each font. (3) `src/config/style-registry.ts`: Updated `GeneralStyles.fonts` type to array format. Updated `STYLE_REGISTRY.general.fonts` default to `[{name: "Playfair Display", isDefault: true}, {name: "DM Sans"}, {name: "Heyam"}]`. (4) `src/lib/apply-style-registry.ts`: Updated to use first font (or font with `isDefault: true`) as both `--font-display` and `--font-body`.
  - **Verification:** `npm run build` succeeds. No linter errors.

- **RegistryEditor Phase 3.2: General tab with color palette matrix, font settings, radius/shadows, live preview**
  - **Plan:** docs/PLAN_REGISTRY_EDITOR.md Phase 3.2 (approved).
  - **Changes:** (1) `src/pages/RegistryEditor.tsx`: Full General tab implementation with Tabs (shadcn/ui). Color palette matrix grid (4×5, 17 cells) with click-to-edit; empty slots show "+ Add" placeholder. Color picker modal (react-colorful HexColorPicker) with hex input, name/comment editing, "Used by" tags from STYLE_VARIABLES_INVENTORY.md. Font settings (display, body, importUrl). Global radius input. Shadow presets (soft, card, cardHover, playful). Live preview style tile showing sample heading, body text, buttons, and color swatches. Two-way sync: in-memory registry state updates CSS vars via `applyStyleRegistry()` on every change (immediate visual feedback). Responsive layout with section borders, max-widths, and mobile-friendly color picker. (2) `package.json`: Added `react-colorful` dependency.
  - **Verification:** `npm run build` succeeds. No linter errors. All Phase 3.2 checklist items completed.

- **Profiles 500 / PostgREST 42P17: documentation and error code in UI**
  - **Failure:** `/admin/registry-editor` shows "Could not verify admin access"; API log shows `proxy_status: "PostgREST; error=42P17"` (PostgreSQL undefined_object).
  - **Changes:** (1) `docs/FAILURE_LOG.md`: new entry for 500 on profiles fetch with 42P17; root cause (missing table/column or RLS referencing missing object); fix steps (create table, align column names, fix RLS). (2) `docs/ADMIN_REGISTRY_EDITOR.md`: new §5 "Error 42P17 (undefined_object)" with SQL checks (table exists, column names, RLS), and optional RLS disable to isolate. (3) `src/hooks/useProfile.ts`: profile fetch error now includes Supabase `code` in the message (e.g. `... (code: 42P17)`) so the "Could not verify admin access" screen shows the code.
  - **Verification:** User follows doc to create/fix `public.profiles` and policies; reload `/admin/registry-editor` to confirm 200 and access.

- **RegistryEditor Phase 2.4, 2.5, 3.1: Section roll-out, dividers from registry, RegistryEditor route**
  - **Plan:** docs/PLAN_REGISTRY_EDITOR.md.
  - **2.4 Roll-out:** `apply-style-registry.ts`: section background CSS vars for why, howItWorks, compatibilityTest, whatYouReceive, pastEditions, experience, testimonials, pricing, faq, finalCta, footer; nav vars (--nav-link-color, --nav-link-hover-bg, --nav-link-hover-text, --nav-cta-radius). ProblemSection, HowItWorksSection, CompatibilityTestSection, WhatYouReceiveSection, PastEditionsSection, ExperienceSection, TestimonialsSection, PricingSection, FAQSection, FinalCTASection, FooterSection: section elements use `bg-[hsl(var(--*-section-bg))]`. Navbar: links and CTA use registry vars for color and radius.
  - **2.5 Section-owned dividers:** `Index.tsx`: each WaveDivider reads from STYLE_REGISTRY (hero, why, howItWorks, compatibilityTest, whatYouReceive, pastEditions, experience, testimonials, pricing, faq, finalCta). topColor/bottomColor use `hsl(var(--palette-N))`; variant mapped from registry style (wavy→wave1, wavy2→wave2, blob→blob, sawtooth→zigzag).
  - **3.1 RegistryEditor route:** Route `/admin/registry-editor` added. `AdminRoute`: requires auth + `profiles.role = 'admin'` (via `useProfile`); else redirect to login or home. `src/hooks/useProfile.ts`: fetches profile from `public.profiles` by user id. `src/pages/RegistryEditor.tsx`: placeholder page with title and back link. Admin role is set in Supabase `profiles.role` (see docs/supabase_phase1_profiles_orders_rls.sql).
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG entry.

- **RegistryEditor Phase 2.1–2.3: Runtime bridge + Hero pilot**
  - **Plan:** docs/PLAN_REGISTRY_EDITOR.md (Phase 2).
  - **2.1 Runtime bridge:** `src/lib/apply-style-registry.ts`: `applyStyleRegistry(registry)` injects palette as `--palette-N` and semantic vars (--background, --primary, etc.), --radius, --font-display/--font-body, shadows, gradients; injects font `importUrl` via link tag. `src/main.tsx`: calls `applyStyleRegistry(STYLE_REGISTRY)` on bootstrap.
  - **2.2 Fonts:** Registry fonts applied via link tag and --font-display, --font-body on root.
  - **2.3 Hero pilot:** `apply-style-registry.ts`: added `getPaletteHsl(cells, index)`; sets hero-specific vars (--hero-section-bg, --hero-pattern-opacity, badge/quote/heading/description/footer/buttons/image). `src/components/HeroSection.tsx`: section background, pattern opacity, badge, quote, heading, description, primary/secondary buttons, footer text, and hero image container use CSS vars from registry. Visual parity preserved (same palette indices as before).
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG entry.

- **RegistryEditor Phase 1: Style registry and inventory**
  - **Plan:** docs/PLAN_REGISTRY_EDITOR.md (approved).
  - **Changes:** (1) `docs/STYLE_VARIABLES_INVENTORY.md`: full audit of stylized elements with proposed registry paths and palette index → “used by” tags for color modal. (2) `src/config/style-registry.ts`: STYLE_REGISTRY with position-based palette (cells with hex, name?, comment?), General (fonts, radius, shadow, gradient), section-owned dividers (wavy/wavy2/sawtooth/blob + top/bottom color index), image style model, and per-section styles (nav, hero, why, howItWorks, compatibilityTest, whatYouReceive, pastEditions, experience, testimonials, pricing, faq, finalCta, footer, login, register, forgotPassword, resetPassword, dashboard, checkout, account). Types: PaletteCell, SectionDivider, ImageStyle, GeneralStyles, section types, StyleRegistry.
  - **Verification:** `npm run build` succeeds. No FAILURE_LOG entry (no failure).

- **Fix 502 Bad Gateway on create-checkout-session (Stripe errors visible)**
  - **Failure:** 502 when Stripe threw (e.g. wrong Price ID). Function reads secrets from **Supabase Dashboard**, not `.env.local`. Logged in `docs/FAILURE_LOG.md`.
  - **Changes:** (1) `supabase/functions/create-checkout-session/index.ts`: on Stripe `catch`, return **200** with `{ url: null, error: message }` so the client can show the error. (2) `docs/STRIPE_EDGE_FUNCTION_SETUP.md`: step 9 "If you get 502" — clarify that secrets are in Supabase only; use Price IDs (`price_...`); link to function logs. (3) `docs/FAILURE_LOG.md`: 502 entry added.
  - **Verification:** Redeploy function; set correct Price IDs in Supabase secrets; re-test checkout; error message should appear in UI or in function logs.

- **Fix 401 Unauthorized on create-checkout-session (Stripe Edge Function)**
  - **Failure:** `POST .../functions/v1/create-checkout-session` returned 401; gateway rejected request before function ran. Logged in `docs/FAILURE_LOG.md`.
  - **Changes:** (1) `src/lib/checkout.ts`: explicit `Authorization: Bearer <session.access_token ?? anon_key>` sent on every invoke so the header is always set. (2) `supabase/config.toml`: `[functions.create-checkout-session] verify_jwt = false` so the function accepts calls without JWT verification (function only creates Stripe Checkout session; payment on Stripe). (3) `docs/STRIPE_EDGE_FUNCTION_SETUP.md`: step 8 added for 401 case and redeploy. (4) `docs/FAILURE_LOG.md`: created with root cause, reproduction, fix attempt.
  - **Verification:** User must redeploy for config to apply: `supabase functions deploy create-checkout-session`. Then re-test checkout (gift and logged-in flows).

- **Restore deleted files (Vite import resolution)**
  - `src/config/app-registry.ts`, `src/pages/Checkout.tsx`, and `docs/CHANGELOG_AI.md` had been deleted in the working tree (uncommitted), causing Vite errors: "Failed to resolve import ./pages/Checkout" and "Failed to resolve import @/config/app-registry". Restored all three from Git with `git restore`. Build and dev server now resolve imports. Documented in changelog; no FAILURE_LOG (root cause: accidental/uncommitted deletion; fix: restore from Git).

- **Supabase auth flow (login, register, forgot password, reset password, protected routes)**
  - Supabase client in `src/lib/supabase.ts` (env: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`).
  - `.env.example` with placeholder Supabase env vars.
  - `AuthContext` and `useAuth` hook: `user`, `loading`, `signIn`, `signOut`, `signUp`, `resetPasswordForEmail`, `updatePassword`.
  - `ProtectedRoute`: redirects unauthenticated users to `/login?redirect=<path>`.
  - Auth pages (all copy from `app-registry.ts`): Login, Register, ForgotPassword, ResetPassword.
  - Dashboard (protected): welcome, email/name/member since, subscription/next box placeholders, quick actions, Log Out / Back to Home.
  - Checkout and Account pages (protected placeholders).
  - App routes: `/login`, `/register`, `/forgot-password`, `/reset-password`, `/dashboard`, `/checkout`, `/account`; Dashboard, Checkout, Account wrapped in `ProtectedRoute`.
  - Navbar: when logged in shows user name/email and Log Out; when logged out shows Log In and Get Started (→ `/register`). Nav Get Started href set to `/register`.
  - Login redirect: after sign-in, redirects to `?redirect=` path when present, else `/`.
  - Reset password: reads session from Supabase (recovery link); invalid/expired link shows message and Back to Login.
  - Tests: `src/pages/Login.test.tsx` (render form with app-registry copy, forgot password link); Supabase mocked in test so no env required.

- **Dashboard profile view and edit**
  - App registry: dashboard keys for edit profile (editProfileTitle, editProfileButton, profileNameLabel/Placeholder, saveProfile/saveProfileLoading, cancelEdit, profileUpdatedSuccess, profileUpdateError, changePasswordLabel/changePasswordHref).
  - AuthContext: `updateProfile(fullName)` calling `supabase.auth.updateUser({ data: { full_name } })`.
  - Dashboard: single Account Information card with view mode (email, name, member since, Edit profile button, Change password link) and edit mode (name input, Save, Cancel); success toast on save, inline error on failure; same style (rounded-lg border bg-card, font-display, primary/outline buttons).
  - Tests: `src/pages/Dashboard.test.tsx` (renders welcome and account info, shows Edit profile and Change password; Supabase mocked with session user).

- **Hero rotatingQuotes regression: normalize list and add guardrail tests**
  - **Issue:** `hero.rotatingQuotes` in `app-registry.ts` was accidentally reduced to only the third phrase, and later migrated from an array to an object with numeric keys. `HeroSection` still treated it as a plain array (`data.rotatingQuotes.length` and index access), so changes in the registry could silently drop quotes or break rotation.
  - **Changes:** (1) `HeroSection` now imports and uses `registryListToArray()` so `rotatingQuotes` can safely be either an array or an object with numeric keys; rotation uses the normalized list length and no-ops when there are zero quotes. (2) Added `HeroSection.test.tsx` with a test that asserts `registryListToArray` is called when rendering, and a guardrail test that ensures `APP_REGISTRY.hero.rotatingQuotes` always yields at least three quotes when normalized. (3) Installed `@testing-library/dom` as a devDependency (with `--legacy-peer-deps`) so Vitest + React Testing Library can run.
  - **Files touched:** `src/components/HeroSection.tsx`, `src/components/HeroSection.test.tsx`, `package-lock.json`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm test -- HeroSection.test.tsx` passes; no new linter errors; manual check of the hero on `/` shows all three rotating questions cycling correctly.

- **Past Editions: carousel layout instead of static grid**
  - **Issue:** The `pastEditions` section previously rendered as a static 2×4/4×2 grid of text-only cards. You requested a more visual, carousel-style layout inspired by an image-based example.
  - **Changes:** (1) Replaced the grid in `PastEditionsSection` with a responsive horizontal carousel: on desktop, narrow side rails expand to a wide center panel when active; on small screens, items stack but still behave as a tap-to-activate carousel. (2) Each edition now renders as a `li` with `aria-current` tracking the active item; clicking any edition updates the active index and triggers a smooth width/overlay transition using a short-lived `--transition` CSS custom property on the wrapper list. (3) Added `PastEditionsSection.test.tsx` to ensure there is exactly one list item per registry edition, that the center item is active by default, and that clicking another item updates `aria-current`. (4) Added a small IntersectionObserver polyfill in `src/test/setup.ts` so Framer Motion’s `useInView` works under Vitest/jsdom.
  - **Files touched:** `src/components/PastEditionsSection.tsx`, `src/components/PastEditionsSection.test.tsx`, `src/test/setup.ts`, `docs/CHANGELOG_AI.md`.
  - **Verification:** `npm test -- HeroSection.test.tsx PastEditionsSection.test.tsx` passes; `npm run lint` reports no new issues; manual check of the Past Editions section shows a smooth, clickable carousel with the center edition emphasized.

- **Testimonials orbit carousel & Past Editions: full registry coverage for text/images/colors**
  - **Issue:** After adding the orbiting testimonials carousel and the Past Editions expanding carousel, a few literals (emoji prefixes, fallback avatar URL) and some section-specific colors still lived in components instead of in `app-registry.ts` / `style-registry.ts`, and the existing `STYLE_REGISTRY.pastEditions.cards` / `STYLE_REGISTRY.testimonials.cards` entries were not yet used by the new layouts.
  - **Changes:** (1) `src/config/app-registry.ts`: added `pastEditions.subtitleEmoji`, `testimonials.subtitleEmoji`, `testimonials.fallbackAvatar`, and ensured `testimonials.people[*].quote` is present for each scientist so central quotes are registry-driven. (2) `src/components/PastEditionsSection.tsx`: reads `subtitleEmoji` from the registry instead of a hardcoded `📚`; imports `useStyleRegistry()` and applies per-card background colors via `backgroundColor: hsl(var(--pastEditions-card-{i}-bg))` based on `registry.pastEditions.cards`. (3) `src/components/TestimonialsSection.tsx`: reads `subtitleEmoji` and `fallbackAvatar` from the registry; uses a `makeSafeImage(fallback)` helper so fallback image behavior is configurable; imports `useStyleRegistry()` and applies testimonial card background colors via `backgroundColor: hsl(var(--testimonials-card-{i}-bg))` using `registry.testimonials.cards` and the current `activeIndex`.
  - **Verification:** `npm test -- HeroSection.test.tsx PastEditionsSection.test.tsx TestimonialsSection.test.tsx` passes (6 tests). Manual smoke check confirms that (a) emoji prefixes, quotes, and fallback avatar are all editable via the registry, and (b) Past Editions and Testimonials card fills respond to style changes made in `RegistryEditor`.

- **Dashboard Quick Actions: dedicated pages and `/account` route deprecation**
  - **Goal:** Consolidate account functionality into the authenticated Dashboard and give each Quick Action (Order History, Subscription, Settings, Notifications) a real, protected destination, while deprecating the standalone `/account` route.
  - **Changes:** (1) `src/config/app-registry.ts`: Uses existing `dashboard.orderHistoryHref`, `subscriptionActionHref`, `settingsHref`, and `notificationsHref` plus the `orderHistory`, `subscriptionManagement`, `settings`, and `notifications` sections as single sources of truth for new pages. (2) `src/pages/Dashboard.tsx`: Quick Action links now use `to={data.orderHistoryHref}`, `to={data.subscriptionActionHref}`, `to={data.settingsHref}`, and `to={data.notificationsHref}` so they navigate to real routes. (3) `src/pages/OrderHistory.tsx`, `SubscriptionManagement.tsx`, `SettingsPage.tsx`, `NotificationsPage.tsx`: New registry-driven pages composed from `Navbar` + padded main + centered content, using `useRegistryContent().getSectionContent("<sectionKey>")` for headings and copy. (4) `src/App.tsx`: Added protected routes for `/orders`, `/subscription`, `/settings`, and `/notifications`, each wrapped in `ProtectedRoute`; removed the `/account` route so all account navigation flows through Dashboard + Quick Actions. (5) `src/pages/Account.tsx`, `src/pages/Account.test.tsx`: Left in place as legacy, non-routed page; updated the test to use `getAllByText("Test User")` so it remains stable with the Navbar’s logged-in label.
  - **Files touched:** `src/config/app-registry.ts`, `src/pages/Dashboard.tsx`, `src/pages/Dashboard.test.tsx`, `src/pages/OrderHistory.tsx`, `src/pages/SubscriptionManagement.tsx`, `src/pages/SettingsPage.tsx`, `src/pages/NotificationsPage.tsx`, `src/pages/OrderHistory.test.tsx`, `src/pages/SubscriptionManagement.test.tsx`, `src/pages/SettingsPage.test.tsx`, `src/pages/NotificationsPage.test.tsx`, `src/pages/Account.test.tsx`, `src/App.tsx`, `docs/PLAN_DASHBOARD_QUICK_ACTIONS.md`, `workflow/ChangeLog.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm test` passes for the full Vitest suite, including the updated `Dashboard.test.tsx` and new page tests. Manual reasoning: all new destinations are lightweight, read-only views behind `ProtectedRoute` that rely only on `useAuth` (indirectly via Navbar) and `useRegistryContent`, with no additional network calls or stateful side effects.

- **Remove Lovable page branding from HTML metadata**
  - **Goal:** Remove the leftover `Lovable App` branding from the page shell so browser tabs and social metadata reflect the actual product name.
  - **Changes:** (1) Added `src/index-metadata.test.ts` as a guardrail test that reads the root `index.html` and fails if Lovable branding remains. (2) Updated `index.html` metadata to use `KumiBox` title, author, Open Graph title/description, and Twitter title/description. (3) Updated the packaged HTML copies in `Hostinger/index.html` and `Loveable/index.html` to keep generated output aligned with the source page metadata.
  - **Files touched:** `src/index-metadata.test.ts`, `index.html`, `Hostinger/index.html`, `Loveable/index.html`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- src/index-metadata.test.ts` passes.

- **Social sign-in: Google and Facebook via Supabase Auth**
  - **Goal:** Add Google/Facebook sign-in options to the existing auth flow without creating a separate user system, so social logins still use Supabase `auth.users` and the existing `public.profiles` trigger pipeline.
  - **Changes:** (1) `src/contexts/AuthContext.tsx`: added `signInWithOAuth(provider)` for `google` and `facebook`, using `supabase.auth.signInWithOAuth` with a safe redirect target derived from the current URL’s `?redirect=` parameter when present, else `/`. (2) `src/components/SocialAuthButtons.tsx`: new shared, reusable auth UI block with divider + outline buttons for Google/Facebook. (3) `src/pages/Login.tsx` and `src/pages/Register.tsx`: added social auth section, reused the shared component, and surfaced provider errors through existing inline error messaging. (4) `src/config/app-registry.ts`: added registry-driven copy for the divider, provider button labels, and provider error message on both auth pages. (5) `src/test/render-with-auth.tsx`: extended the auth test helper with a default `signInWithOAuth` stub. (6) Added/updated focused tests in `src/contexts/AuthContext.test.tsx`, `src/pages/Login.test.tsx`, and new `src/pages/Register.test.tsx`.
  - **Files touched:** `src/contexts/AuthContext.tsx`, `src/contexts/AuthContext.test.tsx`, `src/components/SocialAuthButtons.tsx`, `src/pages/Login.tsx`, `src/pages/Login.test.tsx`, `src/pages/Register.tsx`, `src/pages/Register.test.tsx`, `src/config/app-registry.ts`, `src/test/render-with-auth.tsx`, `workflow/FAIL_LOG.md`, `workflow/ChangeLog.md`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- src/pages/Login.test.tsx src/pages/Register.test.tsx src/contexts/AuthContext.test.tsx` passes; `npm run build` passes; live browser verification on `http://127.0.0.1:8081/login` and `/register` confirmed Google/Facebook buttons render cleanly at desktop (`1280x800`) and mobile (`390x844`) without overlap.

- **Temporary Google-only social-auth rollout**
  - **Goal:** Hide Facebook from users until its credentials are ready, while keeping the underlying OAuth implementation intact so it can be re-enabled later without reworking the auth layer.
  - **Changes:** (1) Updated `src/components/SocialAuthButtons.tsx` to support conditional provider rendering with `showFacebook`. (2) Set `showFacebook={false}` in `src/pages/Login.tsx` and `src/pages/Register.tsx` so only Google is shown in the live UI. (3) Updated `src/pages/Login.test.tsx` and `src/pages/Register.test.tsx` to assert that Google remains visible, Facebook is absent, and the Google button still dispatches `signInWithOAuth("google")`. (4) Recorded the red-phase failure caused by Facebook still rendering in `workflow/FAIL_LOG.md` and `docs/FAILURE_LOG.md`.
  - **Files touched:** `src/components/SocialAuthButtons.tsx`, `src/pages/Login.tsx`, `src/pages/Register.tsx`, `src/pages/Login.test.tsx`, `src/pages/Register.test.tsx`, `workflow/FAIL_LOG.md`, `workflow/ChangeLog.md`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- src/pages/Login.test.tsx src/pages/Register.test.tsx src/contexts/AuthContext.test.tsx` passes (7 tests); `npm run build` passes; browser smoke test on `http://127.0.0.1:8081/login` and `/register` confirmed the Google button is visible, no Facebook button is rendered, and clicking Google redirects successfully to `https://accounts.google.com/...` with Supabase callback `https://rtnispswkyybiliynezz.supabase.co/auth/v1/callback`.

- **Coming soon page moved to `/coming-soon` with registry support**
  - **Goal:** Bring over the committed coming-soon landing page without replacing the live full-site homepage, and make its copy/media editable through the existing registry workflow.
  - **Changes:** (1) Added `comingSoon` content to `src/config/app-registry.ts`, including metadata, launch date, hero copy, countdown labels, feature pills, and social CTA. (2) Added `comingSoon` style support to `src/config/style-registry.ts`, `src/lib/apply-style-registry.ts`, and `src/pages/RegistryEditor.tsx`, including page background styling and editable hero image path. (3) Created `src/pages/ComingSoon.tsx` as a registry-driven version of the imported landing page, reusing the existing hero image resolver and setting document title/meta description from the registry. (4) Updated `src/App.tsx` so the full site stays at `/` and the coming-soon experience lives at `/coming-soon`. (5) Added a preview shortcut in `src/components/AdminLayout.tsx`. (6) Added regression tests in `src/pages/ComingSoon.test.tsx`, `src/components/AdminLayout.test.tsx`, and `src/App.test.tsx`. (7) Logged the split-text matcher false start in `docs/FAILURE_LOG.md`.
  - **Files touched:** `src/config/app-registry.ts`, `src/config/style-registry.ts`, `src/lib/apply-style-registry.ts`, `src/pages/RegistryEditor.tsx`, `src/pages/ComingSoon.tsx`, `src/components/AdminLayout.tsx`, `src/App.tsx`, `src/pages/ComingSoon.test.tsx`, `src/components/AdminLayout.test.tsx`, `src/App.test.tsx`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** Focused verification passed with `npm run test -- --run src/pages/ComingSoon.test.tsx src/components/AdminLayout.test.tsx src/App.test.tsx`; final full-suite verification passed with `npm run test` (25 files, 55 tests).

- **Google sign-in: add provider logo affordance**
  - **Goal:** Make the `Continuar con Google` button easier to recognize by adding an immediate visual Google cue in the shared social-auth UI.
  - **Changes:** (1) Added `src/components/SocialAuthButtons.test.tsx` as the red-phase coverage for the shared auth button block, asserting the Google action includes a logo and still dispatches the correct provider. (2) Updated `src/components/SocialAuthButtons.tsx` to render an inline Google SVG icon inside the Google button, so both login and register inherit the improvement automatically without changing auth behavior.
  - **Files touched:** `src/components/SocialAuthButtons.tsx`, `src/components/SocialAuthButtons.test.tsx`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- --run src/components/SocialAuthButtons.test.tsx src/pages/Login.test.tsx src/pages/Register.test.tsx` passed (8 tests); no new linter errors.

- **Coming soon countdown target updated to March 21 at noon**
  - **Goal:** Make the coming-soon countdown point to the requested launch moment of March 21 at `12:00 PM`.
  - **Changes:** (1) Added a focused regression assertion in `src/pages/ComingSoon.test.tsx` to lock the countdown target to the expected registry timestamp. (2) Updated `APP_REGISTRY.comingSoon.launchDateIso` in `src/config/app-registry.ts` from the old March 1 timestamp to `2026-03-21T12:00:00`, which the existing countdown component already consumes automatically.
  - **Files touched:** `src/config/app-registry.ts`, `src/pages/ComingSoon.test.tsx`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- --run src/pages/ComingSoon.test.tsx` passed (3 tests); no new linter errors.

- **Brand logo system: replace fox emoji with real image and make logo pickers editable in RegistryEditor**
  - **Goal:** Use the real `Logo sin BG.png` asset everywhere the brand logo appears, and stop modeling those logo occurrences as editable emoji text.
  - **Changes:** (1) Replaced `nav.logoEmoji` and `comingSoon.brand.emoji` with registry image-path fields in `src/config/app-registry.ts`, both defaulting to `Logo sin BG.png`. (2) Added `src/components/BrandLogo.tsx` as a shared logo renderer built on top of the existing registry image resolver, so logo occurrences consistently resolve bundled or `/public` assets. (3) Updated `src/components/Navbar.tsx` and `src/pages/ComingSoon.tsx` to render the real brand logo image instead of the fox emoji. (4) Updated `src/pages/RegistryEditor.tsx` to expose dedicated image pickers for the navigation logo and the coming-soon brand logo, and to hide those image-path fields from the generic text-content editor so they are managed as images instead of copy. (5) Added regression coverage in `src/components/Navbar.test.tsx`, `src/pages/ComingSoon.test.tsx`, and `src/pages/RegistryEditor.test.tsx`.
  - **Files touched:** `src/config/app-registry.ts`, `src/components/BrandLogo.tsx`, `src/components/Navbar.tsx`, `src/pages/ComingSoon.tsx`, `src/pages/RegistryEditor.tsx`, `src/components/Navbar.test.tsx`, `src/pages/ComingSoon.test.tsx`, `src/pages/RegistryEditor.test.tsx`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- --run src/components/Navbar.test.tsx src/pages/ComingSoon.test.tsx src/pages/RegistryEditor.test.tsx` passed (5 tests); final verification with `npm run test` passed for the full Vitest suite (28 files, 60 tests); no new linter errors.

- **Coming soon follow-up: migrate stale saved overrides and align brand lockup**
  - **Goal:** Fix the remaining live-page issues where the coming-soon screen could still show the old fox icon, a zeroed countdown, and a slightly misaligned title/logo pair even after the registry schema was updated.
  - **Changes:** (1) Added a normalization step to `src/contexts/RegistryContentContext.tsx` so saved overrides are migrated on load: legacy `nav.logoEmoji` and `comingSoon.brand.emoji` values are converted to the current image-path fields, and the old default coming-soon launch timestamp (`2026-03-01T10:08:00`) is upgraded to the current March 21 noon target. The cleaned overrides are written back to localStorage so the app self-heals instead of requiring a manual reset. (2) Updated the coming-soon brand lockup in `src/pages/ComingSoon.tsx` to use a flex row with centered alignment and added a stable hook for regression coverage. (3) Expanded `src/pages/ComingSoon.test.tsx` with page-level coverage for the aligned brand lockup and the stale-override migration path.
  - **Files touched:** `src/contexts/RegistryContentContext.tsx`, `src/pages/ComingSoon.tsx`, `src/pages/ComingSoon.test.tsx`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- --run src/pages/ComingSoon.test.tsx` passed (5 tests); final verification with `npm run test` passed for the full Vitest suite (28 files, 62 tests); no new linter errors.

- **Hostinger deployment folder refreshed from the latest production build**
  - **Goal:** Prepare the current site for manual Hostinger upload by replacing the stale `Hostinger` export with the newest production build while keeping a rollback-ready backup of the previous payload.
  - **Changes:** (1) Ran a fresh `vite build` to generate the current production bundle in `dist/`. (2) Moved the full previous `Hostinger` contents into `History/Hostinger-2026-03-11_171929` as a point-in-time backup. (3) Replaced `Hostinger` with the new `dist` output so the upload folder now reflects the latest app version. (4) Restored the existing SPA rewrite rules by copying `.htaccess` back into `Hostinger`, because the Vite build outputs `_redirects` but not Hostinger-specific rewrite config.
  - **Files touched:** `Hostinger/**`, `History/Hostinger-2026-03-11_171929/**`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run build` passed; filesystem verification confirmed the refreshed `Hostinger` folder contains the new build files plus `.htaccess`, and `History` contains the timestamped backup folder.

- **How-it-works CTA removed and Hostinger footer delivery link fixed**
  - **Goal:** Remove the `Ve un ejemplo de rutina` CTA from the `Como Funciona` section and fix the live `Envíos` footer link so it targets `#delivery-windows` in production.
  - **Changes:** (1) Updated `src/config/app-registry.ts` so `howItWorks.ctaButton` is now empty instead of advertising the sample-routine CTA. (2) Updated `src/components/HowItWorksSection.tsx` to render the header CTA only when both a label and an href are present, preventing empty or legacy CTA data from producing a button. (3) Added `src/components/HowItWorksSection.test.tsx` to lock the CTA removal in the rendered UI. (4) Expanded `src/config/get-default-registry.test.ts` to catch drift between source defaults and the public registry snapshot for the how-it-works CTA and the footer `Envíos` link. (5) Regenerated `public/registry.json` from the current source registries so production no longer serves the stale footer `href=\"#\"` snapshot. (6) Rebuilt the app and refreshed `Hostinger` again, archiving the previous upload set to `History/Hostinger-2026-03-11_180844`.
  - **Files touched:** `src/components/HowItWorksSection.tsx`, `src/components/HowItWorksSection.test.tsx`, `src/config/app-registry.ts`, `src/config/get-default-registry.test.ts`, `public/registry.json`, `Hostinger/**`, `History/Hostinger-2026-03-11_180844/**`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- --run src/components/HowItWorksSection.test.tsx src/config/get-default-registry.test.ts` passed (3 tests); `npm run build` passed; `npm run test` passed for the full Vitest suite (29 files, 64 tests); browser verification on `http://127.0.0.1:4173` confirmed the CTA is absent and clicking footer `Envíos` updates the hash to `#delivery-windows` and scrolls to the section.

- **Checkout pricing return links switched to real anchors**
  - **Goal:** Make `Volver a planes` reliably return users from checkout flows to the pricing section instead of depending on router-only hash navigation.
  - **Changes:** (1) Added `src/pages/CheckoutNavigation.test.tsx` to lock the pricing return behavior for both `Checkout` and `CheckoutCancel`. (2) Replaced the imperative pricing back actions in `src/pages/Checkout.tsx` and `src/pages/CheckoutCancel.tsx` with real anchor links pointing to `/#pricing`, while leaving the rest of the checkout routing behavior unchanged.
  - **Files touched:** `src/pages/Checkout.tsx`, `src/pages/CheckoutCancel.tsx`, `src/pages/CheckoutNavigation.test.tsx`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- --run src/pages/CheckoutNavigation.test.tsx` passed (2 tests); no new linter errors.

- **Hash-based landing-page navigation now scrolls after route changes**
  - **Goal:** Make `/#pricing` and similar in-page links work reliably when reached from other routes like checkout, instead of only updating the URL and staying at the top of the page.
  - **Changes:** (1) Added `src/components/HashScrollHandler.tsx`, a router-aware effect that watches `location.hash`, finds the matching element, and calls `scrollIntoView` after route changes. (2) Mounted the handler once in `src/App.tsx` so all hash-based route transitions inherit the fix. (3) Added `src/components/HashScrollHandler.test.tsx` to reproduce the checkout-to-home hash navigation flow and lock the scrolling behavior in tests.
  - **Files touched:** `src/components/HashScrollHandler.tsx`, `src/components/HashScrollHandler.test.tsx`, `src/App.tsx`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run test -- --run src/components/HashScrollHandler.test.tsx src/pages/CheckoutNavigation.test.tsx` passed (3 tests); no new linter errors.

- **Hostinger deploy folder refreshed from the latest build with in-place history backup**
  - **Goal:** Update the `Hostinger` upload folder to the newest production-ready page files while preserving the previous generated deploy surface for rollback.
  - **Changes:** (1) Added `src/hostinger-deploy-sync.test.ts`, a regression test that reads `dist/index.html`, extracts the current JS/CSS entry bundles, and verifies `Hostinger/index.html` plus `Hostinger/assets` match that build output. (2) Ran a fresh `npm run build` to generate the current production bundle in `dist/`. (3) Archived the previous generated deploy surface by moving `Hostinger/index.html` and `Hostinger/assets/` into `Hostinger/history/20260311-224505/`. (4) Copied the latest `dist/index.html` and `dist/assets/` back into `Hostinger/` so the upload folder now matches the current build. (5) Logged the stale deploy state, the initial hardcoded-bundle test mistake, and the PowerShell command-separator mismatch in the failure logs.
  - **Files touched:** `src/hostinger-deploy-sync.test.ts`, `Hostinger/index.html`, `Hostinger/assets/**`, `Hostinger/history/20260311-224505/**`, `workflow/FAIL_LOG.md`, `workflow/ChangeLog.md`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`.
  - **Verification:** `npm run build` passed; `npm run test -- src/hostinger-deploy-sync.test.ts` passed; filesystem verification confirmed `Hostinger/index.html` now references `/assets/index-Cm9BMrr-.js` and `/assets/index-CNNEYgXU.css`, and the previous deploy surface exists under `Hostinger/history/20260311-224505/`.

- **Homepage SEO copy comparison document prepared for side-by-side review**
  - **Goal:** Give the user a decision-ready document with proposed conversion-first SEO copy changes for the homepage before implementing anything on the page.
  - **Changes:** Added `docs/HOMEPAGE_SEO_COPY_COMPARISON.md`, a review-only document with side-by-side `Current` vs `Proposed` copy for homepage metadata, hero messaging, pricing intro, FAQ rewrites, trust/E-E-A-T support copy, final CTA refinements, and an approval matrix for selective implementation.
  - **Files touched:** `docs/HOMEPAGE_SEO_COPY_COMPARISON.md`, `workflow/ChangeLog.md`, `docs/CHANGELOG_AI.md`.
  - **Verification:** Confirmed the comparison document exists in `docs/` and that no homepage code, component logic, or registry values were modified for this request.

- **One-command Hostinger refresh automation**
  - **Goal:** Replace the manual Hostinger build/archive/copy workflow with a repeatable one-command refresh process.
  - **Changes:** (1) Added `scripts/sync-hostinger.ps1`, a parameterized PowerShell script that archives the current generated Hostinger deploy surface into `Hostinger/history/<timestamp>/` and then copies in the latest `dist/index.html` and `dist/assets/`. (2) Added `hostinger:sync` and `hostinger:refresh` scripts to `package.json`, where `hostinger:refresh` runs the full build + sync flow in one command. (3) Added `src/hostinger-sync-script.test.ts`, which verifies both the npm shortcut and the actual archive/copy behavior by executing the PowerShell script against temporary directories. (4) Logged the missing-script, missing-package-command, and PowerShell `param(...)` placement failures during the TDD cycle.
  - **Files touched:** `scripts/sync-hostinger.ps1`, `package.json`, `src/hostinger-sync-script.test.ts`, `workflow/FAIL_LOG.md`, `workflow/ChangeLog.md`, `docs/FAILURE_LOG.md`, `docs/CHANGELOG_AI.md`, `docs/VMP_COMPLIANCE_AUDIT.md`, `Hostinger/index.html`, `Hostinger/assets/**`, `Hostinger/history/20260312-095329/**`.
  - **Verification:** `npm run test -- src/hostinger-sync-script.test.ts` passed (2 tests); `npm run hostinger:refresh` passed end-to-end; `npm run test -- src/hostinger-sync-script.test.ts src/hostinger-deploy-sync.test.ts` passed (3 tests); filesystem verification confirmed `Hostinger/index.html` now references `/assets/index-MQghwU3t.js` and `/assets/index-CQTMGJSs.css`, and the newest real refresh snapshot exists under `Hostinger/history/20260312-095329/`.